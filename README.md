# Operator Flake

Maybe this will save you some trouble:

Show the template:
```nix
nix flake show github:dandeandean/operator-flake
```

Use the template:
```nix
nix flake init -t github:dandeandean/operator-flake
```
